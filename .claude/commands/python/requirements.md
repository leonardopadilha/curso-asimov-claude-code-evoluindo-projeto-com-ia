---
description: Gera o arquivo requirements.txt com as dependências atuais do pip
---

# Pip Freeze

Execute o comando `pip freeze > requirements.txt` no terminal.

Após a execução, confirme se o arquivo foi gerado corretamente e liste brevemente as 3 primeiras linhas para validação.